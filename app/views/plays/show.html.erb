<h3><%= @play.title %></h3>
<h2> <%= @play.director %></h2>
<p><%= @play.description %></p>
<%= link_to "back", root_path %>
<%= link_to "edit Play", edit_play_path(@play) %>
<%= link_to "delete Play", play_path(@play), method: :delete, data: { confirm: "Are you sure ?"} %>
